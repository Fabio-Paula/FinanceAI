#!/bin/sh
# When git runs from Windows on a WSL path, delegate lint-staged into WSL
if command -v wsl.exe > /dev/null 2>&1; then
  wsl.exe -d Ubuntu -- bash -i -c "cd ~/Github/FinanceAI && ./node_modules/.bin/lint-staged"
else
  ./node_modules/.bin/lint-staged
fi
